# Local .terraform directories
**/.terraform/*
# .terraform.lock.hcl is intentionally COMMITTED (pinned provider versions +
# cross-platform checksums) — do NOT ignore it.

# State files — contain secrets in plaintext, never commit
*.tfstate
*.tfstate.*
*.tfstate.backup

# Variable files with real values (commit terraform.tfvars.example instead)
*.tfvars
*.tfvars.json
!*.tfvars.example

# Crash / plan output
crash.log
crash.*.log
*.tfplan

# CLI config that may hold credentials
.terraformrc
terraform.rc

# macOS
.DS_Store
